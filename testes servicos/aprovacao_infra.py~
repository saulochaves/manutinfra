import urllib , json

ordens = urllib.urlopen('http://localhost:8000/api/ordens_infraestrutura/aprovacao')
ordens_formatadas = json.load(ordens)

for x in range(len(ordens_formatadas)):
	if ordens_formatadas[x]['status'] == "em_aprovacao":
		print "Passou"
	else:
		print "Erro!"
		
